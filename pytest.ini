[pytest]
markers =
    integration: marks tests as integration tests (slower, may require network/disk)
    e2e: marks tests as end-to-end workflow tests (very slow)
    perf: marks tests as performance benchmarks (not run in standard CI) 